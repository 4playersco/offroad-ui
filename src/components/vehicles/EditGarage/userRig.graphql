query USER_RIG {
  user {
    id
    rig {
      image {
        id
        publicId
        url
      }
    }
    vehicle {
      id
      year
      make
      model
      trim
      name
      outfitLevel
      mods
    }
  }
}
